from django.urls import path
# from .views import SensoresList, SensorDetail


app_name = 'AntartidaAPI'

urlpatterns = [
    # path('<int:pk>/', SensorDetail.as_view(name='detailCreate')),
    # path('', SensoresList.as_view(name='listCreate'))
]
